#include <stdio.h>
#include<math.h> 
void main()
{
    int array[10];
    int i, j, a, temp, keynum;
    int low, mid, high;
 
    printf("Enter the value of num \n");
    scanf("%d", &a);
    printf("Enter the elements one by one \n");
    for (i = 0; i <a; i++)
    {
        scanf("%d", &array[i]);
    }
    printf("Input array elements \n");
    for (i = 0; i <a; i++)
    {
        printf("%d\n", array[i]);
    }
    printf("the sorted list of array");
    }
    }
